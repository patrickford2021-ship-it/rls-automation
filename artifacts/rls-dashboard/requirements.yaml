packages:
  "framer-motion": "For smooth tab transitions and queue item animations"
  "date-fns": "For formatting timestamps in transcripts and callbacks"
  "clsx": "Utility for constructing className strings"
  "tailwind-merge": "Utility for merging tailwind classes without conflicts"
  "lucide-react": "Icons for the dashboard UI"

images: []

notes:
  - "Using API client generated by Orval in @workspace/api-client-react"
  - "Assuming shadcn/ui components are available in @/components/ui/ but aggressively styled via index.css for terminal look"
